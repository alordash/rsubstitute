use syn::*;
use crate::generation::mock_struct::models::*;

pub(crate) fn generate_for_static_fn() -> MockStruct {
    let result = MockStruct {
    path: todo!(),
    item_struct: todo!()
    };
    return result;
}
